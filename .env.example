DATABASE_URL=postgres://postgres:postgres@localhost:5432/app_db?sslmode=disable
# HS256 signing key — use a long random value in production (32+ bytes).
JWT_SECRET=dev-only-change-me-use-long-random-secret
